name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build surfcam (Debian Bookworm–style image)
        run: docker build -f Dockerfile.build -t surfcam-ci .
